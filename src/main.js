console.log("hello there");

let config = {
    type: Phaser.CANVAS,
    width: 640,
    height: 480,
    title: "Rocket Patrol",
    version: "2.01",
    author: "Kaelen Cook"
}

let game = new Phaser.Game(config);
